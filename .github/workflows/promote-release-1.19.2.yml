name: Promote remaining API 1.19.2 presets

on:
  push:
    branches: [publish/release-1.19.2]
    paths:
      - .github/workflows/promote-release-1.19.2.yml
      - ci/release-1.19.2/**

concurrency:
  group: promote-comfyui-release-1.19.2
  cancel-in-progress: false

jobs:
  promote:
    if: github.repository == 'SaladTechnologies/comfyui-api'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
      actions: read
      packages: write
    env:
      GH_TOKEN: ${{ github.token }}
      IMAGE: ghcr.io/saladtechnologies/comfyui-api
    defaults:
      run:
        shell: bash
    steps:
      - name: Check out the promotion checks
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false

      - name: Verify the release source, tested images, and current tags
        run: python3 ci/release-1.19.2/verify.py before

      - name: Publish stable tags for the four remaining presets
        env:
          DOCKER_CONFIG: ${{ runner.temp }}/ghcr-release-1192-auth
        run: |
          set -euo pipefail
          [[ "$GITHUB_ACTIONS" == true && "$RUNNER_ENVIRONMENT" == github-hosted ]]
          [[ "$DOCKER_CONFIG" == "$RUNNER_TEMP/ghcr-release-1192-auth" ]]
          mkdir -p "$DOCKER_CONFIG"
          trap 'docker logout ghcr.io >/dev/null 2>&1 || true; rm -rf -- "$DOCKER_CONFIG"' EXIT
          printf '%s' "$GH_TOKEN" | docker login ghcr.io --username "$GITHUB_ACTOR" --password-stdin
          python3 - <<'PY'
          import runpy
          import subprocess

          checked = runpy.run_path("ci/release-1.19.2/verify.py")
          for preset, digest in checked["PRESETS"].items():
              if preset == "dreamshaper8":
                  continue
              image = checked["IMAGE"]
              tag = checked["PREFIX"] + preset
              subprocess.run([
                  "docker", "buildx", "imagetools", "create", "--prefer-index=false",
                  "--tag", image + ":" + tag, image + "@" + digest,
              ], check=True)
          PY
          python3 ci/release-1.19.2/verify.py after

      - name: Confirm credential cleanup and report runner disk use
        if: always()
        run: |
          set -euo pipefail
          test ! -e "$RUNNER_TEMP/ghcr-release-1192-auth"
          docker system df
